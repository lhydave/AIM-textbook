\documentclass{standalone}

\input{../../fig-config.tex}

% plots
\usepackage{tikz}
\usetikzlibrary{arrows}
\usetikzlibrary{arrows.meta,positioning,calc,3d}
\usetikzlibrary{automata}
\usepackage{tikz-3dplot}
\usepackage{pgfplots}
\pgfplotsset{compat=newest}
\tikzset{
    punkt/.style={
        rectangle,
        rounded corners,
        draw=black, very thick,
        minimum height=2em,
        inner sep=6pt,
        text centered,
        fill=gray!10
    }
}

\begin{document}
\begin{tikzpicture}[node distance=3cm, auto]
    \node[punkt] (MDP) {MDP};
        \node[punkt, right=of MDP] (MRP) {MRP};
        \node[punkt, right=of MRP] (MC) {Markov链};
        \draw[->, -stealth, very thick] (MDP) to node {固定策略} (MRP);
        \draw[->, -stealth, very thick] (MRP) to node {去掉奖励函数} (MC);
\end{tikzpicture}
\end{document}